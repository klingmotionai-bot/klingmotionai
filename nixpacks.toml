# Use npm install so lock file doesn't have to be in sync (avoids "Missing: cors, dotenv, express-session" on Railway)
[phases.install]
cmds = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = []

[start]
cmd = "node backend/server.js"
